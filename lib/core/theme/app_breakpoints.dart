/// Shared width breakpoints used to switch pages between the desktop/web
/// layout (fixed sidebar, multi-column content) and the mobile layout
/// (drawer navigation, single-column content).
class AppBreakpoints {
  AppBreakpoints._();

  /// Below this width a page should use its mobile layout.
  static const double mobile = 900;
}
